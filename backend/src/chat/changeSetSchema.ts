/** Current independently versioned persistence contracts for ChangeSets and their integration WAL. */
export const CURRENT_CHANGE_SET_SCHEMA_VERSION = 3 as const;
export const CURRENT_CHANGE_SET_WAL_SCHEMA_VERSION = 3 as const;
